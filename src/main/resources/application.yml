spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/semantic_search
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: update
gemini:
  api-key: ${GEMINI_API_KEY}